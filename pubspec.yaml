name: selectable_autolink_text
description: Generate inline links that can be selected and tapped in text for Flutter.
version: 2.6.0
homepage: https://github.com/miyakeryo/flutter_selectable_autolink_text

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
